#include "main.h"

/**
 * display_prompt - displays prompt for user
 * @prompt: personalised prompt to be displayed
 *
 * takes prompt string as an argument and
 * prints to standard output
 *
 * Return: None
 */

void display_prompt(const char *prompt)
{
	/*when called it prints prompt*/
	printf("%s ", prompt);
}
